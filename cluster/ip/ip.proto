syntax = "proto3";

package tinymake.cluster;

// L3 Subnet.
message Subnet {
  // Network Address, e.g. 10.2.0.0
  string address = 1;
  // Subnet mask, e.g. 16
  int32 netmask = 2;
  // Child subnets
  repeated Subnet children = 3;
}
